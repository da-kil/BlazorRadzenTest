apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "beachbreak-queryapi.fullname" . }}
  labels:
    {{- include "beachbreak-queryapi.labels" . | nindent 4 }}
data:
  appsettings.json: |
    {
      "Logging": {
        "LogLevel": {
          "Default": "Information",
          "Microsoft.AspNetCore": "Warning"
        }
      },
      "AllowedHosts": "*"
    }
