@using ti8m.BeachBreak.Client.Models
@using Radzen
@using Radzen.Blazor

@if (ShouldShowComments())
{
    <div class="review-comments-section mb-4">
        <div class="rc-title mb-3">
            <RadzenIcon Icon="comment" Class="me-1" />
            <span>Review Comments</span>
        </div>

        @if (ShouldShowManagerReviewSummary())
        {
            <div class="rc-entry manager-entry mb-3">
                <div class="rc-meta">
                    <span class="rc-label rc-label-info">Manager Review Summary</span>
                    <span class="rc-author">@(Assignment?.ManagerReviewFinishedByEmployeeName ?? "manager") &middot; after finishing the review meeting</span>
                    @if (Assignment?.ManagerReviewFinishedDate.HasValue == true)
                    {
                        <span class="rc-date">@Assignment.ManagerReviewFinishedDate.Value.ToLocalTime().ToString("MMM dd, yyyy HH:mm")</span>
                    }
                </div>
                <div class="rc-body rc-body-info">
                    @if (!string.IsNullOrWhiteSpace(Assignment?.ManagerReviewSummary))
                    {
                        <span style="white-space: pre-wrap;">@Assignment.ManagerReviewSummary</span>
                    }
                    else
                    {
                        <span class="rc-empty">No review summary provided</span>
                    }
                </div>
            </div>
        }

        @if (ShouldShowEmployeeComments())
        {
            <div class="rc-entry employee-entry mb-3">
                <div class="rc-meta">
                    <span class="rc-label rc-label-success">Employee Comments</span>
                    <span class="rc-author">@Assignment?.EmployeeName &middot; when confirming the review outcome</span>
                    @if (Assignment?.EmployeeReviewConfirmedDate.HasValue == true)
                    {
                        <span class="rc-date">@Assignment.EmployeeReviewConfirmedDate.Value.ToLocalTime().ToString("MMM dd, yyyy HH:mm")</span>
                    }
                </div>
                <div class="rc-body rc-body-success">
                    @if (!string.IsNullOrWhiteSpace(Assignment?.EmployeeReviewComments))
                    {
                        <span style="white-space: pre-wrap;">@Assignment.EmployeeReviewComments</span>
                    }
                    else
                    {
                        <span class="rc-empty">No comments provided</span>
                    }
                </div>
            </div>
        }

        @if (ShouldShowManagerFinalNotes())
        {
            <div class="rc-entry final-entry mb-3">
                <div class="rc-meta">
                    <span class="rc-label rc-label-warning">Manager Final Notes</span>
                    <span class="rc-author">@(Assignment?.FinalizedByEmployeeName ?? "manager") &middot; when finalizing (internal, not visible to employee)</span>
                    @if (Assignment?.FinalizedDate.HasValue == true)
                    {
                        <span class="rc-date">@Assignment.FinalizedDate.Value.ToLocalTime().ToString("MMM dd, yyyy HH:mm")</span>
                    }
                </div>
                <div class="rc-body rc-body-warning">
                    @if (!string.IsNullOrWhiteSpace(Assignment?.ManagerFinalNotes))
                    {
                        <span style="white-space: pre-wrap;">@Assignment.ManagerFinalNotes</span>
                    }
                    else
                    {
                        <span class="rc-empty">No final notes provided</span>
                    }
                </div>
            </div>
        }
    </div>
}


@code {
    [Parameter]
    public QuestionnaireAssignment? Assignment { get; set; }

    [Parameter]
    public bool IsEmployee { get; set; }

    [Parameter]
    public bool IsManager { get; set; }

    private bool ShouldShowComments()
    {
        if (Assignment == null) return false;

        return Assignment.WorkflowState is
            WorkflowState.ReviewFinished or
            WorkflowState.EmployeeReviewConfirmed or
            WorkflowState.Finalized;
    }

    private bool ShouldShowManagerReviewSummary()
    {
        if (Assignment == null) return false;

        return Assignment.WorkflowState is
            WorkflowState.ReviewFinished or
            WorkflowState.EmployeeReviewConfirmed or
            WorkflowState.Finalized;
    }

    private bool ShouldShowEmployeeComments()
    {
        if (Assignment == null) return false;

        return Assignment.WorkflowState is
            WorkflowState.EmployeeReviewConfirmed or
            WorkflowState.Finalized;
    }

    private bool ShouldShowManagerFinalNotes()
    {
        if (Assignment == null) return false;

        if (IsEmployee && !IsManager)
            return false;

        return IsManager && Assignment.WorkflowState == WorkflowState.Finalized;
    }
}
