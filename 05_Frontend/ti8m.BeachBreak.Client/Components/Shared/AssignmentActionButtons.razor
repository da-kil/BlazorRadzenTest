@using Radzen
@using Radzen.Blazor
@using ti8m.BeachBreak.Client.Models
@inherits CategoryOptimizedTranslatableComponent

<div class="template-actions">
    @if (ShowEditAction && CanEdit())
    {
        <RadzenButton ButtonStyle="ButtonStyle.Light"
                     Icon="edit"
                     Size="ButtonSize.Small"
                     Title="@T("tooltips.edit-assignment")"
                     Click="@(() => OnActionClick.InvokeAsync(new AssignmentActionEventArgs { Assignment = Assignment, Action = "edit" }))" />
    }

    @if (ShowReminderAction && CanSendReminder())
    {
        <RadzenButton ButtonStyle="ButtonStyle.Light"
                     Icon="notifications"
                     Size="ButtonSize.Small"
                     Title="@T("tooltips.send-reminder")"
                     Click="@(() => OnActionClick.InvokeAsync(new AssignmentActionEventArgs { Assignment = Assignment, Action = "remind" }))" />
    }

    @if (ShowCancelAction && CanCancel())
    {
        <RadzenButton ButtonStyle="ButtonStyle.Light"
                     Icon="cancel"
                     Size="ButtonSize.Small"
                     Title="@T("tooltips.cancel-assignment")"
                     Click="@(() => OnActionClick.InvokeAsync(new AssignmentActionEventArgs { Assignment = Assignment, Action = "cancel" }))" />
    }

    @if (ShowManageViewersAction && CanManageViewers())
    {
        <RadzenButton ButtonStyle="ButtonStyle.Light"
                     Icon="group"
                     Size="ButtonSize.Small"
                     Title="@T("tooltips.manage-viewers")"
                     Click="@(() => OnActionClick.InvokeAsync(new AssignmentActionEventArgs { Assignment = Assignment, Action = "manage-viewers" }))" />
    }
</div>

@code {
    [Parameter] public QuestionnaireAssignment Assignment { get; set; } = default!;
    [Parameter] public EventCallback<AssignmentActionEventArgs> OnActionClick { get; set; }
    [Parameter] public bool ShowEditAction { get; set; } = true;
    [Parameter] public bool ShowReminderAction { get; set; } = true;
    [Parameter] public bool ShowCancelAction { get; set; } = true;
    [Parameter] public bool ShowManageViewersAction { get; set; }

    protected override string[] GetRequiredTranslationCategories()
    {
        return new[]
        {
            TranslationCategories.Tooltips
        };
    }

    private bool CanEdit()
    {
        return Assignment.WorkflowState != WorkflowState.Finalized;
    }

    private bool CanSendReminder()
    {
        return Assignment.WorkflowState == WorkflowState.Assigned ||
               Assignment.WorkflowState == WorkflowState.EmployeeInProgress ||
               Assignment.WorkflowState == WorkflowState.ManagerInProgress ||
               Assignment.WorkflowState == WorkflowState.BothInProgress;
    }

    private bool CanCancel()
    {
        return Assignment.WorkflowState == WorkflowState.Assigned;
    }

    private bool CanManageViewers()
    {
        return !Assignment.IsWithdrawn;
    }

    public class AssignmentActionEventArgs
    {
        public QuestionnaireAssignment Assignment { get; set; } = default!;
        public string Action { get; set; } = string.Empty;
    }
}